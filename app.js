const WORKER_URL = "https://currency.digitalimarketingchannel.workers.dev/"; const directLinks = { bonbast: "https://bonbast.com/", bonbast2: "https://www.bon-bast.com/", navasan: "https://www.navasan.net/", tgju: "https://www.tgju.org/currency" }; function setState(card,text,kind){const p=card.querySelector(".price"); p.textContent=text; p.classList.remove("loading","error"); if(kind)p.classList.add(kind);} async function checkOne(name){ const card=document.querySelector(`[data-source="${name}"]`); if(name!=="tgju"){ setState(card,"Tap to open site",null); card.onclick=()=>window.open(directLinks[name],"_blank"); return; } setState(card,"Checking...","loading"); try{ const res=await fetch(`${WORKER_URL}?site=${name}&t=${Date.now()}`,{cache:"no-store"}); if(!res.ok) throw new Error("HTTP "+res.status); const data=await res.json(); if(!data.price) throw new Error("No price found"); setState(card,data.price,null); }catch(e){ setState(card,"Failed: open site directly","error"); card.onclick=()=>window.open(directLinks[name],"_blank"); }} document.querySelectorAll(".card").forEach(card=>{card.addEventListener("click",()=>checkOne(card.dataset.source));}); document.getElementById("checkAllBtn").addEventListener("click",()=>{ Object.keys(directLinks).forEach(checkOne); });